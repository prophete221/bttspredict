'use client'

// FloatingElements removed in v9 — decorative floating shapes added visual noise
// without contributing to conversion. The new "Quantum Edge" design is cleaner
// and more focused on the content that matters: predictions, promo code, CTAs.
export default function FloatingElements() {
  return null
}
